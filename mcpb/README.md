# speech-mcp — MCPB bundle root

This directory is the pack root for `mcpb pack` (Claude Desktop .mcpb).
Multi-provider speech gateway: FunASR STT, Gemini Live/TTS, Hume AI, ElevenLabs, wake word.

## Build

From repository root:
```powershell
just mcpb-pack
```
